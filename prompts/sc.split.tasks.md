Split the final spec into a set of detailed tasks that can be implemented in parallel. Place the results into a set of files in a new subdirectory. Files shall be named as `docs/tasks/<spec-name>/task-<task-number>-<task-name>.md`.

Mark each task with a completion mark (`[ ]`).

Also, add a summary file in the same subdirectory. The summary shall provide guidance for orchestration and parallel execution.

